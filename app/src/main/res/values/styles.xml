<resources>

    <!-- Base application theme. -->
    <style name="TemaGeral">
        <item name="android:icon">@drawable/logo</item>
        <item name="android:actionBarStyle">@android:style/Widget.Holo.Light.ActionBar</item>
        <item name="android:logo">@drawable/logo</item>
        <item name="android:displayOptions">useLogo|showHome</item>
        <item name="android:textColor">#fff</item>
        <item name="android:shadowDx">3</item>
        <item name="android:shadowDy">3</item>
        <item name="android:shadowRadius">3</item>
        <item name="android:shadowColor">#000</item>
    </style>

    <style name="TemaGeral.TemaLogin">
        <item name="android:logo">@android:color/transparent</item>
    </style>

    <style name="TemaGeral.TemaPrincipal">
    </style>

    <style name="TemaGeral.TemaTarefa">
    </style>

</resources>
